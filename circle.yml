machine:
  node:
    version: 6.1.0

dependencies:
  pre:
    - npm install -g npm@5.1.0
    - npm install ethereumjs-testrpc@4.0.1
    - npm install -g truffle@3.4.3

test:
  override:
    - node node_modules/ethereumjs-testrpc/bin/testrpc:
        background: true
    - npm run test
